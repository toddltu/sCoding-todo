{% extends 'base.html.twig' %}

{% block title %}Update Todo{% endblock %}

{% block body %}
    <div class="card mt-5" style="margin: 0 auto; width: 20vw; min-width: 400px; max-width: 600px;">
        <div class="card-body">
            <h3 class="card-title float-left">Update item</h3>
            <div class="float-right">{{ include('api/_delete_form.html.twig') }}</div>
            <div style="clear: both"></div>
            <div class="card-text">
                {{ include('api/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>
{% endblock %}